# Copy to .env and fill in real values. .env is gitignored.

# Required to run the agent at all.
OPENAI_API_KEY=sk-...

# Optional — omit to run with LLM Observability disabled (no traces emitted).
DD_API_KEY=
DD_SITE=datadoghq.com

# Optional overrides.
DD_LLMOBS_ML_APP=stock-watchlist-agent
